%p
	%b Notebook:
	= @notebook.title

%p
New Note

%p
= simple_form_for [@notebook, @note] do |f|
	= f.input :title
	= f.input :content, :input_html => {:rows=>10, :class => "span8"}
	= f.button :submit